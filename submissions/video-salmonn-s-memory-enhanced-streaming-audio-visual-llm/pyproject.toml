[project]
name = "video-salmonn-s-repro"
version = "0.1.0"
description = "CPU-only evidence audit for video-SALMONN S ICML 2026 reproduction"
requires-python = ">=3.10"
dependencies = ["pytest"]

[tool.pytest.ini_options]
testpaths = ["tests"]
