[project]
name = "vggt-motion-icml2026-repro"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "gradio>=5.44.0",
  "numpy>=1.26.0",
]

[dependency-groups]
dev = [
  "pytest>=8.0.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
