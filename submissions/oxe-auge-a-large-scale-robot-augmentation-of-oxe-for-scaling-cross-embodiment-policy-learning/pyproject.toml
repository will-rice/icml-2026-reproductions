[project]
name = "oxe-auge-repro"
version = "0.1.0"
description = "CPU-only reproduction audit for OXE-AugE policy learning artifacts"
requires-python = ">=3.10"
dependencies = [
  "huggingface-hub>=0.34.0",
  "pytest>=8.0.0",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
