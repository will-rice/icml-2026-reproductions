[project]
name = "neural-thickets-repro"
version = "0.1.0"
description = "CPU-only reproduction evidence for Neural Thickets"
requires-python = ">=3.11"
dependencies = [
  "pytest>=8.0",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
