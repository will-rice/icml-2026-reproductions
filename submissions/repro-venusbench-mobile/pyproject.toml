[project]
name = "venusbench-mobile-repro"
version = "0.1.0"
description = "ICML 2026 reproduction evidence for VenusBench-Mobile"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "gradio>=5.0",
    "pytest>=8.4",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
