# Speculative Decoding Benchmark

Run SPEED-Bench qualitative:

```bash
python run.py --dataset_path data/speed/qualitative
```

Run SPEED-Bench throughput:

```bash
python run.py --dataset_path data/speed/throughput_1k
```

The framework records `timing.json` and `acceptance_rate.json`.
