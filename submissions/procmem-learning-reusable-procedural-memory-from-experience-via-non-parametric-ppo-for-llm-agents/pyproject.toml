[project]
name = "procmem-repro"
version = "0.1.0"
description = "CPU-only ProcMEM reproduction evidence for ICML 2026"
requires-python = ">=3.10"
dependencies = ["pytest>=8"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
