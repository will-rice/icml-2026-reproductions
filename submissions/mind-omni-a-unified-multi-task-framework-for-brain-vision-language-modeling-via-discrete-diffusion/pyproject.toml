[project]
name = "mind-omni-repro"
version = "0.1.0"
description = "CPU-only evidence audit for Mind-Omni artifacts"
requires-python = ">=3.10"
dependencies = [
  "pytest>=8.0.0",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
