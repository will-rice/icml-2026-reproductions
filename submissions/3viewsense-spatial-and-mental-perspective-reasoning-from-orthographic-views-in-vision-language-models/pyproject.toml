[project]
name = "viewsense-reproduction"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "gradio>=5.0.0",
]

[dependency-groups]
dev = ["pytest>=8.4.0"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
