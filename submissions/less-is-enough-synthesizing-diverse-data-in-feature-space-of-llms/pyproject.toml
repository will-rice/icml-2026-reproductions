[project]
name = "fac-synthesis-reproduction"
version = "0.1.0"
description = "CPU-only evidence bundle for FAC Synthesis"
requires-python = ">=3.11"
dependencies = ["gradio>=5"]

[dependency-groups]
dev = ["pytest>=8"]

[tool.uv]
package = false
