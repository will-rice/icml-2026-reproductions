# MedMosaic Reproduction Report

Attempt `32f2eefb-2f74-40c6-a98d-f7d9eed154e6` audits the released MedMosaic dataset artifact for paper `OMdQJQwp26`.

The primary dataset artifact is pinned to `icml-anon-submission/medmosaic-dataset@a6ea67bd4a65b87248c6651e559656b2c31fa669`. The evidence reads only `data/test.parquet`, which contains 4,661 rows across seven QA types. This does not reproduce the paper claim that MedMosaic contains 46,701 question-answer pairs.

The pinned index does reproduce the structural category claim: it contains `Sound_Only`, `Speech_Only`, `Speech_Sound`, `Open_Ended`, `voice_qa`, `Long_Form`, and `Multi_Turn` rows. The artifact also contains difficulty labels and audio paths for all rows.

The 13-model benchmark result, audio-removal ablation, per-difficulty model accuracy trends, and 72.4% clinical expert acceptance rate are not reproduced measurements here. The pinned dataset index does not include model evaluation logs, ablation outputs, per-difficulty score tables, or expert review sheets.

Machine-readable evidence is generated by `generate_evidence.py` and written to `evidence/bundle.json`.
