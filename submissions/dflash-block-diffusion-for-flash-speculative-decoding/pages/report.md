# DFlash Reproduction Report

Attempt `9bdedd5e-7e42-43cc-a356-bab8f5d9c344` audits the pinned DFlash source tree for paper `Oz335dV48X`.

The primary code artifact is `z-lab/dflash@94e4abc5e0c31b67bc1a9d30f1cc34ece28a8756`. The source audit verifies the implementation-level mechanism claim: the code extracts target hidden features, conditions draft attention on `target_hidden`, uses non-causal draft attention, drafts a token block from mask/noise embeddings, and verifies the accepted prefix against target logits.

The GPU speedup claims are inconclusive in this submission. No Qwen3 non-thinking benchmark, thinking-mode benchmark, SGLang FA4 throughput run, LongBench long-context run, or draft-depth/feature-count/block-size/KV-injection ablation was executed. The evidence bundle intentionally leaves `reproduced_speedup_measurements` and `reproduced_ablation_measurements` empty.

Machine-readable evidence is generated by `generate_evidence.py` and written to `evidence/bundle.json`.
