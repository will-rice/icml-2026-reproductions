[project]
name = "vcg-bench-reproduction"
version = "0.1.0"
description = "CPU evidence for VCG-Bench structural benchmark claims"
requires-python = ">=3.10"
dependencies = [
    "gradio>=4.0.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
