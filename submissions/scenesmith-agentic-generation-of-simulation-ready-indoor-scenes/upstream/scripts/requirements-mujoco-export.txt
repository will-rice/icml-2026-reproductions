# Dependencies for scripts/export_scene_to_mujoco.py with USD export
# Use this in a separate venv (incompatible with bpy due to pxr conflict)
#
# Setup:
#   ./scripts/setup_mujoco_export.sh
#
# Usage:
#   source .mujoco_venv/bin/activate
#   python scripts/export_scene_to_mujoco.py <scene_dir> -o output/ --usd
#   python scripts/export_scene_to_mujoco.py --sdf model.sdf -o output/ --usd

mujoco==3.3.5
numpy>=1.26,<2.0
trimesh>=4.0.0
Pillow>=10.0.0
scipy>=1.10.0
drake>=1.44.0
omegaconf>=2.3.0
lxml>=5.0.0
mujoco-usd-converter==0.1.0a3
