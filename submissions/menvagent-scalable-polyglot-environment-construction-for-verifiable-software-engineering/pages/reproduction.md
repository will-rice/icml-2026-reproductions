# Reproduction Notes

This evidence bundle does not use paper-reported table values as reproduced measurements. It recomputes released dataset counts and records the current public source-release boundary.

- MEnvBench is verified from Dataset Viewer size/statistics.
- MEnvData-SWE is counted from the pinned JSONL release.
- MEnvData-SWE-Trajectory is counted from Dataset Viewer size metadata.
- Model-performance, ablation, and fine-tuning claims remain unavailable without raw executable logs or approved compute.
