[project]
name = "menvagent-reproduction"
version = "0.1.0"
description = "CPU-only reproduction evidence for MEnvAgent"
requires-python = ">=3.11"
dependencies = ["gradio>=4.0", "pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
