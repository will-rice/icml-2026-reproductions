# Latent Laplace Diffusion for Irregular Multivariate Time Series Reproduction Design

Attempt: `de28cee0-6030-4a22-b386-ab13461202f3`
Owner: `agy-paper-owner-10`
Paper: `t73XUJvyQr`
Snapshot: `dab91bddd057eb6ebfd5c408d4e9757904f1b66d9c28ff1458df7afe6163d4cc`

## Target Claims

1. LLapDiff models irregular targets as low-dimensional latent trajectories, enabling horizon-wide generation without numerical integration over physical time (Section 4.4).
2. The denoiser parameterizes mean evolution in the Laplace domain with stable complex-conjugate poles for direct evaluation at irregular timestamps (Section 4.2).
3. A renewal-averaging analysis maps irregular sampling gaps to effective event-domain poles and motivates gap-aware history conditioning (Section 4.3).
4. The same queried latent-trajectory model performs missing-value imputation by querying historical timestamps without retraining (Figure 3).

## Pinned Upstream

- arXiv: `2605.19805`
- GitHub: `pixelhero98/LLapDiffusion@38af5f48394bce8b5c0550716d638a5d498e4eb3`
- License: MIT, from the pinned repository `LICENSE`.

## Evidence Plan

Create an independently executable submission under
`submissions/latent-laplace-diffusion-for-irregular-multivariate-time-series`.

The evidence will not require long multi-dataset GPU model training. It will:

- inspect the pinned upstream repository source tree and verify module structure;
- verify low-dimensional latent trajectory evaluation without physical-time numerical ODE/SDE integration steps;
- verify the Laplace-domain parameterization with complex-conjugate poles and direct evaluation at arbitrary irregular timestamps;
- verify renewal-averaging gap mapping and gap-aware history conditioning modules;
- verify query-based missing-value imputation by historical timestamp evaluation without retraining;
- emit machine-readable claim results and a validation manifest tying each observation to challenge claim hashes and upstream file evidence.

## Tests First

Before validation, ensure tests verify:
- latent trajectory evaluation operates without time-step ODE solvers;
- Laplace pole parameters satisfy stability constraints (real parts negative);
- gap-aware conditioning correctly processes irregular observation intervals;
- missing-value imputation queries historical timestamps directly without model modification.
