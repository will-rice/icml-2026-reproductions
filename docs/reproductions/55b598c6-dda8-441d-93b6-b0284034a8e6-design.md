# QuArch: A Benchmark for Hardware Architecture Problem Solving

Attempt: `55b598c6-dda8-441d-93b6-b0284034a8e6`
Paper: `yU6X1XZl8t`
Owner: `agy-paper-owner-04`
Snapshot: `3f2004855f8d64d47eee11381ef246d6374abfb09b82b5b10d879c22152c0205`

## Scope

This reproduction targets two independently testable CPU evaluation claims for the QuArch benchmark:

1. QuArch contains 2,671 expert-validated computer-architecture question-answer pairs built from synthetic generation, crowdsourcing, and academic exams (Figure 3).
2. The benchmark evaluates four skills: Recall, Analyze, Design, and Implement, with relevant context and figures when appropriate (Figure 2).

The attempt will implement deterministic dataset verification, schema validation, skill distribution auditing, and evaluation pipelines on CPU.

## Upstream Pins

- Paper: `arxiv:2510.22087v1`
- Source Pin: `arxiv-source:2510.22087v1`

## Evidence Plan

Create `submissions/quarch-a-benchmark-for-hardware-architecture-problem-solving/` with an independently executable Python package and Space source.

Evidence commands will:
- load and validate the QuArch dataset schema, total record count (2,671 QA pairs), and source distribution breakdown (synthetic, crowdsourcing, academic exams);
- audit the 4-skill categorization (Recall, Analyze, Design, Implement) and verify context/figure metadata bindings.

## Tests First

Before implementation, add failing pytest coverage for:
- claim binding and bundle schema;
- dataset item count and category breakdown verification;
- skill categorization distribution (Recall, Analyze, Design, Implement) validation.

## Outputs

The evidence bundle will include:
- `evidence/bundle.json` with claim results, statuses, observations, tolerances, commands, environment, upstream pins, and file hashes;
- `README.md` and `pages/*.md` summaries;
- a Gradio/markdown Space entrypoint (`app.py`) surfacing the bundle and claim statuses.

## Validation

Controller validation will execute the submission pytest suite, evidence generation, root pytest excluding NAPE, and clean-worktree checks through `attest-validation`. Publication will use dedicated Space `wrice/repro-quarch-yU6X1XZl8t` with required tags.
