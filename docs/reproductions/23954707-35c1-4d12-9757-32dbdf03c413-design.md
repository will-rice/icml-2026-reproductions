# High-accuracy sampling for diffusion models and log-concave distributions

Attempt: `23954707-35c1-4d12-9757-32dbdf03c413`
Paper: `71132`
Owner: `agy-paper-owner-01`
Snapshot: `647c396ada6e95caa7f8711e63dad91456f19e97bd63ec6e9e9303898fe4495b`

## Scope

This reproduction targets three independently testable CPU mathematical/algorithmic sampling claims:

1. The diffusion sampler attains delta-error in polylog(1/delta) steps given sufficiently accurate score estimates, improving the dependence on accuracy over prior high-accuracy samplers (Theorem 4.3).
2. When the data distribution has intrinsic dimension d*, the complexity reduces to Õ(d* polylog(1/delta)) (Corollary 4.4).
3. The same framework yields a polylog(1/delta)-accuracy sampler for log-concave and more general isoperimetric distributions using first-order gradient queries (Section 5).

The attempt will implement exact mathematical verification, step-complexity scaling under score accuracy tolerances, and low intrinsic-dimension projection bounds on CPU benchmark instances.

## Upstream Pins

- Paper: `arxiv:2602.01338v2`
- Source Pin: `arxiv-source:2602.01338v2`

## Evidence Plan

Create `submissions/high-accuracy-sampling-for-diffusion-models-and-log-concave-distributions/` with an independently executable Python package and Space source.

Evidence commands will:
- implement polylog(1/delta) diffusion trajectory integrator with high-accuracy score query interface;
- evaluate error vs step count scaling across varying accuracy delta settings to verify polylog(1/delta) step dependence;
- test intrinsic dimension d* subspace projection to verify Õ(d* polylog(1/delta)) complexity reduction;
- test first-order gradient query sampler for log-concave distributions under isoperimetric conditions.

## Tests First

Before implementation, add failing pytest coverage for:
- claim binding and bundle schema;
- polylog(1/delta) step bound verification for diffusion sampling;
- intrinsic dimension d* scaling bound check;
- log-concave gradient query sampler accuracy bound check.

## Outputs

The evidence bundle will include:
- `evidence/bundle.json` with claim results, statuses, observations, tolerances, commands, environment, upstream pins, and file hashes;
- `README.md` and `pages/*.md` summaries;
- a Gradio/markdown Space entrypoint (`app.py`) surfacing the bundle and claim statuses.

## Validation

Controller validation will execute the submission pytest suite, evidence generation, root pytest excluding NAPE, and clean-worktree checks through `attest-validation`. Publication will use dedicated Space `wrice/repro-high-accuracy-sampling-for-diffusion-models-71132` with required tags.
